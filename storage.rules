rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow any authenticated user to read and write to storage
      // This is consistent with removing specific admin roles for simplicity
      allow read, write: if request.auth != null;
    }
  }
}
